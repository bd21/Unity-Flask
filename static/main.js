$(document).ready(function() {
    //make sure the list_order cookie is parsed
    // var order = JSON.parseCookies.get("listOrder"));
    // alert(order.get('0'));
});
//request the data at the current index

//